for i in range(10):
    print("Enter the mid_1 and mid_2 marks")
    marks=input().split()
    marks1=int(marks[0])
    marks2=int(marks[1])
    maximum_marks=marks2
    minimum_marks=marks2
    if marks1>marks2:
        maximum_marks=marks1
    if marks1<marks2:
        minimum_marks=marks1
    average_marks=(0.8*maximum_marks)+(0.2*minimum_marks)
    print(round(average_marks,0))
